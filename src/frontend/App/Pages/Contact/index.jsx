//component
import Meta from "@App/Core/components/Meta";

// component
const Contact = () => {
    return (
        <main>
            <Meta pageTitle="contact" />
            <h1>Contact</h1>
        </main>
    );
};

export default Contact;
